# `yu::tuples::elementwise_binary_predicate_with`

{{ status_mark('yu::tuples::elementwise_binary_predicate_with') }}

```cpp
namespace yu::tuples {
    template <typename Pred, typename Tuple, typename T>
    concept elementwise_binary_predicate_with = see_below;
}
```