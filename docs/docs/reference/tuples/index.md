# Yu.Tuples

Yu.Tuplesは，タプルインタフェースを持つ型(Tuple)を統一的に扱う機能群を提供する．
C++標準ライブラリの`<ranges>`と`<algorithm>`が提供する操作を，RangeではなくTupleに対して同様に適用できる．
例えば，`<ranges>`が提供するRangeアダプタやビュー，`<algorithm>`が提供する各アルゴリズムを，Tupleに対して同じ構文で利用できる．

なお，このライブラリでは，"Tuple"という語を{{ entity_link('tuple') }}コンセプトで定義される用語としている．用語"Tuple"と{{ entity_link('tuple') }}コンセプト，`std::tuple`を混同しないよう注意すること．

## Tupleアクセス

| 名前                      | 説明                                         | 対応バージョン | 状態   |
| ------------------------- | -------------------------------------------- | -------------- | ------ |
| {{ entity_link('get') }}  | Tupleの各要素にアクセスする (CPO)            | -              | 未実装 |
| {{ entity_link('size') }} | Tupleの要素数を取得する (クラステンプレート) | -              | 未実装 |


## Tupleに関連する型へのアクセス

| 名前                              | 説明                                             | 対応バージョン | 状態   |
| --------------------------------- | ------------------------------------------------ | -------------- | ------ |
| {{ entity_link('element_type') }} | Tupleの各要素の型を取得する (クラステンプレート) | -              | 未実装 |

## Tupleコンセプト

| 名前                                  | 説明                                            | 対応バージョン | 状態   |
| ------------------------------------- | ----------------------------------------------- | -------------- | ------ |
| {{ entity_link('tuple') }}            | Tupleを定義するコンセプト (コンセプト)          | -              | 未実装 |
| {{ entity_link('tuple_structured') }} | タプルインタフェースでアクセス可能 (コンセプト) | -              | 未実装 |

## カスタマイゼーションポイント

| 名前                                  | 説明                                                                 | 対応バージョン | 状態   |
| ------------------------------------- | -------------------------------------------------------------------- | -------------- | ------ |
| {{ entity_link('known_tuple') }} | 構造検査を省略してTupleとみなす型を明示するフラグ (変数テンプレート) | -              | 未実装 |